name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # WeasyPrint's system dependencies (Pango/HarfBuzz) and the Liberation
      # fonts the bundled themes are designed around — see README "Установка".
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libpango-1.0-0 libpangoft2-1.0-0 libharfbuzz-subset0 \
            fonts-liberation poppler-utils

      - name: Install faktura-printer
        run: pip install -e '.[dev]'

      - name: Run tests
        run: pytest -q
